type Callback = (data: Record<string, number>) => Promise<void>;

export default class CrvConcurrentReader {
    public async process(path: string, callback: Callback): Promise<void> {
        
    }
}
